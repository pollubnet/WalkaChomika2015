<Page
    x:Class="WalkaChomika.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WalkaChomika"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

        <StackPanel>
            <ScrollViewer VerticalScrollMode="Enabled" VerticalScrollBarVisibility="Visible" VerticalAlignment="Bottom" HorizontalAlignment="Stretch" Height="200">
                <TextBox x:Name="debug" IsReadOnly="True" AcceptsReturn="True" />
            </ScrollViewer>

            <Button Click="button_Click" x:Name="btnNextTurn" Content="Następna tura!" HorizontalAlignment="Center" />
            <Button Click="btnAutoFight_Click" x:Name="btnAutoFight" Content="Autowalka!" HorizontalAlignment="Center" />
        </StackPanel>
    </Grid>
</Page>